<h1>New Ingredient</h1>
<p>If you dont find the ingredient bellow can add hitting the foods button in the nav menu</p>
<%= form_for :ingredient, url: ingredients_path do |f| %>
   <%= f.label :food %><br>
   <%= f.select :food_id, @foods.all.map{|food| ["#{food.name} - #{food.measurement_unit}",food.id] }%><br>
   <%= f.label :quantity%><br>
   <%= f.number_field :quantity, value:0%><br>
   <%= f.hidden_field :recipe_id, value: params[:recipe_id] %>
   <p>
    <%= f.submit %>
  </p>
<% end %>